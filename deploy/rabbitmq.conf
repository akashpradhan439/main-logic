# Bind AMQP to localhost only (same-VM app/workers only).
listeners.tcp.1 = 127.0.0.1:5672

# Optional: management UI on localhost (access via SSH tunnel).
# Uncomment after enabling rabbitmq_management plugin.
# management.tcp.ip = 127.0.0.1
# management.tcp.port = 15672
